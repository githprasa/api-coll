meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{auth_url}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "john@example.com",
    "password": "password123"
  }
}
